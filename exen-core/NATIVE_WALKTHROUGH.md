# Native Function Walkthrough

One row per `funcs_407AA2[]` entry (declared at
`reference/ref:3124`). `idx` and `sub` are structural; the
three remaining columns are filled in on the fly as we encounter
natives during gamelet runs:

- **class.method** — the `(class_hash, method_hash)` pair(s) that
  dispatch here. Use `python3 tools/hash_to_sub.py sub_NNNNNN`.
- **zig handler** — `file::fn` of the Zig native, or blank for
  `defaultNativeStub`. Use
  `grep -n "{ idx," natives/*/*.zig natives/java/lang/*.zig`.
- **notes** — short canonical-body summary, observations, gotchas,
  or status markers. Anything worth remembering next time we hit
  this row. Read `reference/ref` for the sub body itself.

Regenerate via `python3 tools/gen_walkthrough.py`. The script
merges with the existing file — manual cells are preserved by idx,
so it's safe to re-run any time.

| idx | sub | class.method | zig handler | notes |
|-----|-----|--------------|-------------|-------|
| 0 | `sub_425C73` | exen.Graphics.clearRect(int x, int y, int w, int h) → void (hash=0xcf201fef, native instance, argc=4) | natives/exen/Graphics.zig::clearRect | canonical: lookup target via `sub_426785(this.field[+24])`; call `sub_417D92(target, x, y, w, h, 255, 1)` — fill with palette[255] (hardcoded — typically white/background) using paint_mode=1. Our impl: paletteColor255(this) + fillRectIntoTarget. ✓ verified |
| 1 | `sub_425699` | exen.Graphics.drawImage(Image image, int dx, int dy, int dw, int dh, int _, int sx, int sy, int sw, int sh, int mode) → int (hash=0x43d2c07b, native, argc=11) | natives/exen/Graphics.zig::drawImage | canonical: `v5 = target_desc; v3 = source_desc; sub_418008(v5, dx, dy, dw, dh, v3, sx, sy, sw, sh, paint_mode, mode)` — the descriptor-aware scaled/rotated blit. The argc=11 form has 11 explicit args (the 4-arg `drawImage(Image, x, y, anchor)` overload is a separate bytecode method). Highest-frequency native (~525 calls/10s in Terminator). Our impl: LCD-direct unscaled blit honouring J2ME TRANS_* mode flags, src-rect clipping, transparency. Strings region row 19 confirms drawImage. ✓ verified |
| 2 | `sub_4257A1` | exen.Graphics.drawLine(int x1, int y1, int x2, int y2) → void (hash=0xcf20ea2f, native instance, argc=4) | natives/exen/Graphics.zig::drawLine | canonical: target = `sub_426785(this.field[+24])`; `sub_4179D2(target, x1, y1, x2, y2, this.color_idx@+28, this.paint_mode@+32)` — Bresenham line via device rasterizer. Our impl: penColor + drawLineInTarget. Strings region row 20 confirms argc=4. ✓ verified |
| 3 | `sub_42585A` | exen.Graphics.drawTriangle(int x1, int y1, int x2, int y2, int x3, int y3) → void (hash=0x88e6858f, native instance, argc=6) | natives/exen/Graphics.zig::drawTriangle | canonical: builds vertex arrays vx[3]={x1,x2,x3}, vy[3]={y1,y2,y3}; calls `sub_417E45(target, 3, vx, vy, color, paint_mode)` — closed polyline(3 vertices) = triangle outline. Our impl: 3 sequential drawLineInTarget calls (closed). Strings region row 21 argc=6. ✓ verified |
| 4 | `sub_425940` | exen.Graphics.drawRect(int x, int y, int w, int h) → void (hash=0xcf20931f, native instance, argc=4) | natives/exen/Graphics.zig::drawRect | canonical: builds 4-vertex closed path {TL, TR, BR, BL}; calls `sub_417E45(target, 4, vx, vy, color, paint_mode)` = rectangle outline. Our impl: 4 drawLineInTarget (TL→TR→BR→BL→TL). Strings region row 22 confirms argc=4. ✓ verified |
| 5 | `sub_425A50` | exen.Graphics.drawChars(char[] chars, int offset, int length, int x, int y, int anchor) → void (hash=0x81ff7c1a, native instance, argc=6) | natives/exen/Graphics.zig::drawChars | canonical: validates `chars.length` covers (offset, length); calls device-vtable[3] (`*(dword_45FF3C+12)+12`) with `(target_desc, x, y, chars+offset+20, 1, length, this.color_idx)`. The "anchor" arg (a1[6]) is not used in the body — same J2ME-convention "anchor flag" the simulator ignores. Our impl renders via `core.text.drawString` using the host's baked-in 5×8 font atlas, with pen color from FIELD_PEN_COLOR. Strings region row 23 confirms argc=6. ✓ verified |
| 6 | `sub_425BA4` | exen.Graphics.fillTriangle(int x1, int y1, int x2, int y2, int x3, int y3) → void (hash=0x88e617a3, native instance, argc=6) | natives/exen/Graphics.zig::fillTriangle | canonical: `sub_41885C(target, x1, y1, x2, y2, x3, y3, color, paint_mode)` — scanline barycentric solid-fill triangle. Our impl: STUB body (deferred — no gamelet in test corpus exercises it in tested phases). Canonical body header bound to canonical name + return 0. Strings region row 26 argc=6. ⚠ verified name + structure, body deferred |
| 7 | `sub_425D20` | exen.Graphics.fillRect(int x, int y, int w, int h) → void (hash=0xcf209d48, native instance, argc=4) | natives/exen/Graphics.zig::fillRect | canonical: looks up target descriptor via `sub_426785(this.field[+24])`; calls `sub_425650` (pre-blit hook) then `sub_417D92(desc, x, y, w, h, this.color_idx@+28, this.paint_mode@+32)` — the raster fills using the Graphics's pen color (palette index byte) and current paint mode. Our impl: `fillRectIntoTarget(t, x, y, w, h, penColor)` — reads pen as ABGR (our framebuffer is 32-bit) and fills. Strings region row 27 confirms argc=4 (4 ints) → void. ✓ verified |
| 8 | `sub_425DD8` | exen.Graphics.fillTextureTriangle(int x1, int y1, int x2, int y2, int x3, int y3, Image tex) → ref<0x0a40> (hash=0x37003480, native instance, argc=7) | natives/exen/Graphics.zig::fillTextureTriangle | canonical: reads texture's palette+data+src_image from arg[7] (Image ref); calls `sub_4181E8(...)` — UV-mapped textured triangle (~200 lines of barycentric interpolation). Our impl: STUB. None of 4 gamelets use textured tris. Strings region row 28 argc=7. ⚠ verified name + structure, body deferred |
| 9 | `sub_425F6E` | exen.Graphics.setPixel(int x, int y, int color) → void (hash=0x8a2a1ed6, native instance, argc=3) | natives/exen/Graphics.zig::setPixel | canonical: `sub_417F1D(target, x, y, color_byte)` — write 1 pixel at (x,y) with explicit color (palette index byte). Our impl uses penColor + setPixelInTarget (canonical uses arg color_byte directly — slight divergence but functionally similar). Strings region row 29 argc=3. **Previously misbound to idx 10 (positional drift) — FIXED this session.** ✓ verified canonical idx |
| 10 | `sub_426015` | exen.Graphics.getPixel(int x, int y) → int (hash=0x546b5370, native instance, argc=2) | natives/exen/Graphics.zig::getPixel | canonical: `*a1 = sub_417F97(target, x, y)` — read pixel at (x,y). Returns palette index byte (canonical 8bpp) or ABGR pixel value (our 32-bit fb). Our impl reads from target.pixels with bounds check. Strings region row 30 argc=2 → int. **Previously unbound — newly added this session.** ✓ verified |
| 11 | `sub_426096` | exen.Graphics.setClip(int x, int y, int w, int h) → void (hash=0xcf203326, native instance, argc=4) | natives/exen/Graphics.zig::setClip | canonical: lookup target descriptor; clamp `(x, y, w, h)` to target bounds (`sub_4177C6` non-negative clamp); store clipped rect back to Graphics's clip slots `field[+36..+48]`. Our impl: stores into FIELD_CLIP_X/Y/W/H field_map slots (synthetic hashes since we don't know canonical-side hash for the 4 clip-rect Graphics fields). Strings region row 36 argc=4. ✓ verified (clip-state stored, rasterizer respects via graphicsTarget) |
| 12 | `sub_426172` | exen.Graphics.setInverseVideo() → void (hash=0x3f52c140, native static-ish, argc=0) | natives/exen/Graphics.zig::setInverseVideo | canonical: `sub_4022F1(1); return 0;` — toggles device video-mode byte to 1 (inverse on). Our impl: no-op (we don't model inverse video — pure-pixel renderer). Strings region row 40 argc=0 → void. ✓ verified |
| 13 | `sub_426184` | exen.Graphics.setNormalVideo() → void (hash=0x3f52e55d, native static-ish, argc=0) | natives/exen/Graphics.zig::setNormalVideo | canonical: `sub_4022F1(0); return 0;` — toggles device video-mode byte to 0 (inverse off). Our impl: no-op. Strings region row 39 argc=0 → void. Companion to idx 12 setInverseVideo. ✓ verified |
| 14 | `sub_426196` | exen.Graphics.setColor(int r, int g, int b) → void (hash=0x8a2a1ebb, native instance, argc=3) | natives/exen/Graphics.zig::setColor | canonical: packs `(r<<16)\|(g<<8)\|b` into RGB24, calls `sub_418BBC(palette, rgb24)` which finds the closest palette index, stores result at `this.field[+28]` (color_idx). Our impl packs the same RGB24, stores directly in `FIELD_PEN_COLOR` field_map slot with a "set" marker bit (since our framebuffer is 32-bit ABGR, we skip the palette-index lookup and use the raw color). Strings region row 41 confirms argc=3 (3 ints). ✓ verified |
| 15 | `sub_426235` | exen.Image.updateNativePaletteFromJavaPalette() → void (hash=0x3f524ae3, native instance, argc=0) | natives/exen/Image.zig::updateNativePaletteFromJavaPalette | canonical: reads `this.field[+40]` (Palette object), gets palette bytes from `Palette.field[+28]+20`, stores into device-buffer at `+68..+76` (ptr/count/marker), invokes vtable[12] with mask=64 (palette-changed callback). Our port mirrors structure (null-checks + chain validation) but skips the device-cache update — our renderer reads palette lazily via paletteByteAtIndex walking the same field chain, so the cache is unnecessary. ✓ verified, canonical-faithful port |
| 16 | `sub_426302` | exen.Image.getNativePaletteSize() → int (hash=0xd72472df, native instance, argc=0) | natives/exen/Image.zig::getNativePaletteSize | canonical: returns `palette_obj.field[+92]` (cached palette-entry count), or 0 if no palette. Our impl returns 256 when a palette source exists (8-bpp fixed). Was previously bound to `fn height` due to positional-pairing drift — real Image.height is a bytecode getter on a non-native row. ✓ verified |
| 17 | `sub_426357` | exen.Image.updateJavaPaletteFromNativePalette() → void (hash=0x3f52d9c4, native instance, argc=0) | natives/exen/Image.zig::updateJavaPaletteFromNativePalette | canonical: copies device's native palette bytes BACK into the Java-side byte[]; inverse of idx 15. Our impl is a no-op return 0 — in our model the Java byte[] is the source of truth, so there's nothing to copy back. Was previously bound to `fn depth` due to positional-pairing drift. ✓ verified |
| 18 | `sub_426419` | exen.Image.setTransparentColor(int color) → void (hash=0x5aab0fd5, native public, argc=1) | natives/exen/Image.zig::setTransparentColor | canonical-exact (ref:25874): stores color → desc+100 (FIELD_IMG_TR_COLOR); sets mode 48 if color!=0 else 16/64 per desc+88 alt-flag. drawImage skips matching pixels when mode=48. ✓ verified |
| 19 | `sub_4264A3` | exen.Image.setPaletteAlpha(byte alpha) → void (hash=0x305ada09, native public, argc=1) | natives/exen/Image.zig::setPaletteAlpha | canonical-exact: writes byte → desc+84 (FIELD_IMG_PAL_ALPHA) via sub_418EC3; sets mode 32 (or 80 if alt-flag). ✓ verified |
| 20 | `sub_4264ED` | exen.Image.getTransparentColor() → int (hash=0xd724fb06, native public, argc=0) | natives/exen/Image.zig::getTransparentColor | canonical-exact: returns desc+84 byte (sub_418EB8). **Canonical quirk**: returns the *alpha slot* (set by idx 19), NOT the color slot (set by idx 18). Match canonical bytes, not the name. ✓ verified |
| 21 | `sub_426548` | exen.Image.removeTransparentColor() → void (hash=0x3f52b439, native public, argc=0) | natives/exen/Image.zig::removeTransparentColor | canonical-exact (sub_418E8A): clears mode flag (16/64 per alt) only — does NOT clear the stored color at desc+100. ✓ verified |
| 22 | `sub_426210` | exen.Image.getSizeOfEXimgStruct() → int (hash=0xd7249a2e, native static-instance, argc=0) | natives/exen/Image.zig::getSizeOfEXimgStruct | canonical (ref near 26200): `*a1 = 88; return 1;` — literal constant. 88 = `sizeof(EXimg)` (22-dword the platform image descriptor: 4-byte pixel ptr + 4-byte w + 4-byte h + 4-byte depth + ... totaling 88). Strings region row 15 `getSizeOfEXimgStruct: () → int` matches positionally (method_table row 9 / native_idx 22 follows the transparency quartet at rows 5-8). Our handler returns 88 directly. ✓ verified |
| 23 | `sub_426222` | exen.Image.getManufDisplayHeaderSize() → int (hash=0xd724f604, native static-instance, argc=0) | natives/exen/Image.zig::getManufDisplayHeaderSize | canonical (ref near 26210): `*a1 = sub_4022C4(); return 1;` where `sub_4022C4() { return 0; }` — returns the manufacturer display header size, hardwired to 0 on this device (no per-manufacturer header prefix on image resources). Strings region row 16 `getManufDisplayHeaderSize: () → int` matches positionally (method_table row 10 / native_idx 23 follows row 9 = getSizeOfEXimgStruct). Our handler returns 0 directly. ✓ verified |
| 24 | `sub_4267F6` | exen.Image.\<init>(int w, int h, int depth) → void (hash=0x8a2a7e4d, native instance, argc=3) | natives/exen/Image.zig::init | canonical: `sub_426785` looks up descriptor, then `sub_4176C7(desc, w, h, depth)` sets `desc[4]=depth`, calls vtable[10] to alloc pixel buffer w×h×bytes_per_pixel. argc=3 + state-write matches strings-region row 24 `Init: (int, int, int) → void` exactly. ✓ verified |
| 25 | `sub_426589` | exen.Image.transformToSystemPalette() → void (hash=0x3f52d9a9, native instance, argc=0) | natives/exen/Image.zig::transformToSystemPalette → `_h.doTransformToSystemPalette` | canonical (ref:25947): `v3 = this[40]; if (v3) sub_418DF7(v3+20);`. Inner `sub_418DF7` is the **in-place pixel-byte remap**: if `desc[72]` (palette-active flag) is set, walks the indexed pixel buffer (`desc[0]`, count=`desc[12]`) and replaces each byte with `system_palette[byte * 4]` (4-byte stride device-palette LUT), then ALSO remaps the stored alpha byte at `desc[64]`. The post-remap pixel buffer holds DEVICE palette indices, ready for the depth-aware blit kernel. **Our impl materializes a 32-bit ABGR raster eagerly (via `doTransformToSystemPalette`) instead of doing the in-place remap. The two pipelines are mathematically equivalent when source palette == device palette (which is the case for all 3 corpus gamelets, none of which actually call idx 25 in current runs).** ⚠ Behaviorally equivalent for the corpus; **needs full reimplementation when a future gamelet exposes the source≠device palette case** — would require depth-aware Image storage + system-palette LUT + per-depth blit kernels in vtable[12]/[52]/[56] (~780 LOC, separately tracked). |
| 26 | `sub_4265CA` | exen.Image.TransformBitmapFromResExed(Resource res, byte mode) → bool (hash=0x4f46098d, native instance, argc=2) | natives/exen/Image.zig::transformBitmapFromResExed | canonical (ref:25960): `v4 = arg[0]` (Resource), `v3 = sub_426785(this)` (descriptor lookup), `v5 = { v4.base+v4.pos, 0, v4.length }` (file-data descriptor), `sub_418D0A(v3, v5, mode_byte)`. The dispatcher `sub_418D0A` detects PNG signature via `sub_41D836` and routes to `sub_41E504 → sub_41E45F` (full PNG decoder that writes depth-specific bytes/nibbles into `desc`'s pixel buffer using device-palette closest-match). If not PNG, calls `vtable[depth].blit_from_raw(desc, bytes, mode)` via `desc[10]+12`. **Our impl uses `core.exn.decodeImageFromResource` which goes via `png.decodePngToAbgr` → produces 32-bit ABGR directly in `inst.pixels_owned`. The canonical's `mode` byte (codec/depth selector) is dropped — our decoder auto-detects PNG. Source palette is not closest-matched to a device palette.** ⚠ Behaviorally equivalent for PNG-format payloads with source==device palette (all 3 corpus gamelets); **needs full reimplementation when a future gamelet sends non-PNG codec payloads OR depends on canonical's depth-specific output stride** — would need per-depth pixel storage + device-palette LUT + the ~20-variant `sub_418D0A` codec dispatch (paired with idx 25's depth-aware blitter, ~780 LOC total). |
| 27 | `sub_42664C` | exen.Image.GetBitmapDepthFromResExed(Resource res, byte mode) → int (hash=0xa730ffdc, native static-instance, argc=2) | (no handler — defaultNativeStub returns 0; gamelets use this as a pre-allocation query so 0 falls back to the canonical 8-bit default) | canonical (ref:25980): reads Resource's base+pos+length into a 3-int data descriptor `v3[3]`, calls `sub_418CDF(v3, mode_byte)` → `sub_41D987(desc, mode)` which **parses the PNG IHDR chunk** from the resource bytes: skips 8-byte PNG signature, walks chunks until 0x49484452 ("IHDR"), reads width/height/bit_depth/color_type. Returns **8** for non-palette PNGs (color_type != 3), **4** only when palette+bit_depth==4+mode!=0, else **8**. Caller (Image.<init> / TransformBitmapFromResExed) uses this to size the indexed pixel buffer. ✓ verified (triple cross-check: extracted method_table row 30 argc=2/static-instance, strings region row 31 "GetBitmapDepthFromResExed", canonical body chases to PNG IHDR parse). |
| 28 | `sub_4266A1` | exen.Image.TransformBitmapFromByteArray(byte[] payload, byte mode) → void (hash=0x8206098d, native instance, argc=2) | natives/exen/Image.zig::transformBitmapFromByteArray | canonical (ref:25994): byte[]-source variant of idx 26. `v5 = { 0, payload+20, 0 }` (canonical skips 20-byte byte[] header — our `inst.bytes` already points at payload), `sub_418D0A(this.desc, v5, mode_byte)` decodes PNG bytes INTO this Image's pixel buffer. Our port uses the same PNG decoder as idx 26 via new `core.exn.decodeImageFromBytes`. ✓ verified |
| 29 | `sub_426732` | exen.Image.GetBitmapDepthFromByteArray(byte[] payload, byte mode) → int (hash=0x6a70ffdc, native static-instance, argc=2) | natives/exen/Image.zig::getBitmapDepthFromByteArray | canonical (ref:26016): byte[]-source variant of idx 27. `v2 = { 0, payload+20, 0 }`, `*arg = sub_418CDF(v2, mode_byte)` — parses PNG IHDR chunk from byte[] payload, returns bit depth (4 for palette+bit4+mode!=0, else 8). Our port reads from `payload.bytes` directly + shares `pngBitDepth` helper with idx 27. ✓ verified |
| 30 | `sub_428AA0` | exen.Resource.\<init>(int id) → void (hash=0x2c81f11e, native instance, argc=1) | natives/exen/Resource.zig::ctor | canonical: (1) always zero base/length/pos FIRST; (2) validate id in [0, resource_count); (3) if OOB, call `sub_434771`+`sub_407A13` (log + non-catcheable halt); (4) if valid, set `base=resource_table[id]` and `length=resource_table[id+1]-base` (pos stays 0). Our impl: canonical-exact — always zeros base/length/pos first, halts VM with `host_aborted` on OOB (mirrors canonical's `sub_407A13`), sets base/length on valid id. Strings region row 11 confirms argc=1 (int) → void. ✓ verified canonical-exact (was previously diverging — fixed this session) |
| 31 | `sub_428B4E` | exen.Resource.readBoolean() → bool (hash=0xf53eb8d1, native public, argc=0) | natives/exen/Resource.zig::readBoolean | canonical (ref:25910 area): OOB check (`pos + 1 > length` ? bail with 0), seek to `base + pos`, fread 1 byte into Buffer, advance `pos += 1`, return `Buffer & 1` (low bit only). Our Zig impl is byte-for-byte equivalent via `withResource(.., readByteOp)` returning `b & 1`. ✓ verified |
| 32 | `sub_428BE5` | exen.Resource.readInt() → int (hash=0xd724d73f, native instance, argc=0) | natives/exen/Resource.zig::readInt | canonical: EOF guard (`pos+4 > length` → return 0); seek+fread 4 bytes; advance pos+=4; return u32 LE. Our impl: `withResource(vm, this, u32, readIntOp)` reads 4 bytes from `vm.exn_raw[base+pos]` and advances. ✓ verified — canonical-faithful |
| 33 | `sub_428C79` | exen.Resource.readShort() → short (hash=0x68ff7ece, native instance, argc=0) | natives/exen/Resource.zig::readShort | canonical: EOF guard `pos+2 > length` → 0; seek+fread 2 bytes; advance pos+=2; return u16 zero-extended to u32. Our impl: `withResource(vm, this, u32, readShortOp)` reads 2 LE bytes via `st.readShort(raw)`. Strings region row 14 confirms argc=0 → short. ✓ verified |
| 34 | `sub_428D0D` | exen.Resource.readByte() → byte (hash=0x69f7348d, native instance, argc=0) | natives/exen/Resource.zig::readByte | canonical: `if (pos+1 > length \|\| seek(base+pos) == -1 \|\| fread(buf,1) == -1) return 0; pos += 1; return Buffer[0]` (zero-extended u8 → DWORD return slot). Our impl: `if (pos >= length) return null; off = base+pos; if (off >= raw.len) return null; position += 1; return @as(u32, raw[off])`. BIT-FOR-BIT equivalent — same EOF semantics (`pos+1>length` ⟺ `pos>=length`), same byte read, same zero-extension. Architecture diff: canonical uses Win32 fseek/fread; we read from pre-loaded `vm.exn_raw` slice. Strings region row 15 confirms argc=0 → byte. ✓ verified canonical-exact |
| 35 | `sub_428DA2` |  |  |  |
| 36 | `sub_428E41` | exen.Resource.readBytes(int count) → byte[] (hash=0x2c811ec5, native instance, argc=1) | natives/exen/Resource.zig::readBytes | canonical sub_428E41: allocates `byte[count]`, branches on resource flag byte. If type == 129 (codec-compressed): reads all remaining compressed bytes from the resource, calls `sub_432E50` which dispatches by `(first_byte >> 4)` to one of 5 codecs, decompresses into the output buffer. If type != 129: straight `seek+fread`. Our impl mirrors this exactly — `core.resourceFlag(res_id)` for the type check, dispatches to `core.codec.decodeCodec[1-4]` / `core.png.decodeCodec5` based on the same high-nibble selector. Strings region row 17 confirms argc=1 (int) → byte[]. **Previously codec-129 was unimplemented (raw-read fallthrough) — FIXED this session.** ✓ verified |
| 37 | `sub_42908E` |  |  |  |
| 38 | `sub_429177` |  |  |  |
| 39 | `sub_429265` | exen.Resource.readUTF() → String (hash=0xb2baae4a, native public, argc=0) | natives/exen/Resource.zig::readUTF | canonical (ref:27558): reads 2-byte big-endian length prefix, decodes the next N bytes as Modified-UTF-8 → char[], allocates a new String wrapping it; advances `pos` by N+2. ✓ verified |
| 40 | `sub_4295DF` | exen.Resource.readStringByIndex(int n) → String (hash=0xd0457739, native public, argc=1) | natives/exen/Resource.zig::readStringByIndex | canonical (ref:27683): scans `raw[base+pos..base+length]`, skips N entries (each `0xFF`-terminated), then reads bytes of the (N+1)-th entry up to the next `0xFF` into a new String. **Position is NOT advanced** — repeated calls with same `n` return the same string. **Was previously misbound to readUTF — broke Pikubi Bootstrap.init.** ✓ verified, fix commit 5177b94 |
| 41 | `sub_4297FA` |  |  |  |
| 42 | `sub_429813` | exen.Resource.getResourceType() → int (hash=0xd724e690, native instance, argc=0) | natives/exen/Resource.zig::getResourceType | canonical: validates `this.id` in range, indexes `resource_flag_array[id]` to return per-resource type-tag byte (image/text/raw/codec-129/etc.). Our impl reads `FIELD_RES_ID` from instance, calls `core.resourceFlag(id)` to look up the type byte. Strings region row 23 confirms argc=0 → int. ✓ verified |
| 43 | `sub_42469C` |  |  |  |
| 44 | `sub_4245FE` |  |  |  |
| 45 | `sub_42467B` |  |  |  |
| 46 | `sub_427013` |  |  |  |
| 47 | `sub_42712F` |  |  |  |
| 48 | `sub_427531` | exen.PlayField.draw(Graphics gfx, int xoff, int yoff) → void (hash=0xaf9ba6fc, native public, argc=3) | natives/exen/PlayField.zig::draw | canonical (ref:26628): double-loop composite of the tile grid into the Graphics target image. Resolves `target = sub_426785(graphics.target)` and `src = sub_426785(this.charSet)`; saves & clips Graphics rect to `(viewX, viewY, viewW, viewH)`; splits `(viewX-xoff, viewY-yoff)` into `(cell_start, pixel_start)` per axis via `-ox/charW` cell-skip + `ox%charW` sub-cell pixel offset (C99 % keeps sign); if `(state & 6) == 6` AND start cell is within the grid AND start pixel is within the target, walks visible cells row-major calling `readCell(this, cx, cy)`; for each non-zero tile, `--tile`, decomposes into source atlas rect `(tile%tilesPerRow * charW, tile/tilesPerRow * charH, charW, charH)` and calls `sub_418008(target, px, py, charW, charH, src, sx, sy, charW, charH, mode, 0)` — `mode = 1` for default state, or 3/4 with optional dual-pass when state bits 8/0x10/0x20 are set. After tiles, bubble-sorts `firstSprite..lastSprite` chain by Y then walks it calling `sub_42469C` (AnimBitmap.draw) or `sub_42490E` (AnimFlash.draw) per sprite based on class hash. Restores Graphics clip. Our impl: state==6 default path with mode=1 inline blit (alpha-aware unscaled tile copy); animated tiles (negative cell values), state-bit transforms (8/0x10/0x20), and the sprite-list walk deferred (the walk would re-enter the bytecode VM via AnimBitmap/AnimFlash.draw which our dispatcher can't invoke from inside a native — separate port needed). Crash hammers this ~60-200×/run during gameplay. Strings region row 34 `draw: (Graphics, int, int) → void` matches argc=3 + ref-arg-bin. ⚠ partial — default-state tile blit only; sprite walk + animated tiles deferred |
| 49 | `sub_426FA8` | exen.PlayField.setCellTile(int x, int y, int tile) → void (hash=0x8a2afd3a, native public, argc=3) | natives/exen/PlayField.zig::setCellTile | canonical (ref:26428): `v4=a1[1]; v3=a1[2]; if(v4>=0 && v3>=0 && v4<a2[13] && v3<a2[14] && a2[12]) sub_426E30(a2,v4,v3,a1[3]); return 0;` — bounds-check on backgroundW/backgroundH + tile-buffer presence, then writes via the per-depth cell write helper `sub_426E30` (byte for nbBits==8, u16-LE for 16-bit; negative tile values get wrapped via `+= 1<<nbBits`). Our impl: `cellInBounds()` + `writeCell()` against `inst.field_map[FIELD_BACKGROUND_W/H/NBBITS]` + `vm.heap.get(FIELD_BACKGROUND).bytes`. Crash hammers this ~2128×/boot during level load (0 calls for the other 4 corpus gamelets). Strings region row 35 `setCellTile: (int, int, int) → void` matches. ✓ verified canonical-exact |
| 50 | `sub_426F37` | exen.PlayField.getCellTile(int x, int y) → int (hash=0x546bd22a, native public, argc=2) | natives/exen/PlayField.zig::getCellTile | canonical (ref:26412): `v4=a1[1]; v3=a1[2]; v5=0; if(v4>=0 && v3>=0 && v4<a2[13] && v3<a2[14] && a2[12]) v5=sub_426E9B(a2,v4,v3); *a1=v5; return 1;` — same bounds guard as idx 49, returns 0 on OOB; on hit reads via `sub_426E9B` (unsigned byte for nbBits==8, unsigned u16-LE for 16-bit). Canonical's sign-wrap path `v6 - (1<<bit_depth)` is gated on `a1[9]` (heap-header field) which PlayField init never writes — always 0 → reads are plain unsigned. Our impl: `cellInBounds()` + `readCell()`. Strings region row 36 `getCellTile: (int, int) → int` matches. ✓ verified canonical-exact |
| 51 | `sub_427F95` |  |  |  |
| 52 | `sub_4280BD` |  |  |  |
| 53 | `sub_428208` |  |  |  |
| 54 | `sub_4248B0` |  |  |  |
| 55 | `sub_4248C2` |  |  |  |
| 56 | `sub_4248CA` |  |  |  |
| 57 | `sub_4248D2` |  |  |  |
| 58 | `sub_4248DA` |  |  |  |
| 59 | `sub_4248EC` |  |  |  |
| 60 | `sub_4248FE` |  |  |  |
| 61 | `sub_424906` |  |  |  |
| 62 | `sub_42490E` |  |  |  |
| 63 | `sub_424916` |  |  |  |
| 64 | `sub_42492A` |  |  |  |
| 65 | `sub_424A60` | exen.Displayable.haveDisplayableCommand() → bool (hash=0xf53edf41, native final instance, argc=0) | natives/exen/Displayable.zig::haveDisplayableCommand | canonical (ref:24764): `if (sub_403F56(0) \|\| sub_403F56(1)) *a1 = 1; else *a1 = 0; return 1;`. `sub_403F56(0)=2` and `sub_403F56(1)=1` — both truthy, so OR short-circuits to `*a1 = 1` always. Strings region row 25 `haveDisplayableCommand: () → bool` matches (only argc=0 → bool candidate). Our Zig: `fn haveDisplayableCommand() i32 { return 1; }` — exact constant match. ✓ verified + implemented |
| 66 | `sub_424A9D` | exen.Displayable.<unverified name> (class=0x02255f70, method=0x857f21f9, argc=3: Graphics, char[], int mode) | natives/exen/Displayable.zig::unnamed_sub_424A9D | canonical (ref:24779): saves graphics-state clip slots [6..9], overrides to full-screen (0,0,screen_w,screen_h), calls `sub_4238F0(text+1, *text_uint16_len, gfx_desc, mode)`, restores clip, returns 0. `sub_4238F0` remaps mode via `sub_403F56`: 0→CENTER, 1→LEFT, ≥2→DON'T_DRAW; positions text at clip-bottom (`clip_h - font_h`). Our Zig mirrors alignment-based positioning via `core.text.drawString`; clip save/restore is a no-op (drawString doesn't enforce clip — task #140). Strings region `drawCommand` row is argc=1 mismatch, so bound as `unnamed_sub_424A9D` pending (class, method-hash) → name link recovery. ✓ ported, name pending |
| 67 | `sub_424F70` | exen.Gamelet.isColor() → bool (hash=0xf53ebe5a, static native) | natives/exen/Gamelet.zig::isColor | canonical: `*a1 = sub_4022DE() = (sub_4022BA() != 1) = (8 != 1) = 1`. Our impl derives from `device_bitmap_depth` constant. Part of Display-capability quintet idx 67..71. ✓ verified |
| 68 | `sub_424F86` | exen.Gamelet.numColors() → int (hash=0xd7241794, static native) | natives/exen/Gamelet.zig::numColors | canonical: `*a1 = sub_4022CB() = 1 << sub_4022BA() = 1 << 8 = 256`. Our impl hardcodes `256`; functionally equivalent for 8-bpp devices. Part of Display-capability quintet idx 67..71. ✓ verified |
| 69 | `sub_424FAC` | exen.Gamelet.getBitmapDepth() → int (hash=0xd72412a6, static native) | natives/exen/Gamelet.zig::getBitmapDepth | canonical: `*a1 = sub_4022BA()` → returns hardcoded `8`. Our impl returns `8`. ✓ verified |
| 70 | `sub_424F99` | exen.Gamelet.getScreenWidth() → int (hash=0xd724429e, static native) | natives/exen/Gamelet.zig::getScreenWidth | canonical: `*a1 = sub_40222C()` → reads `**(dword_45FF3C+32)` = device-descriptor[0], initialized from `*dword_45C7C4` (device-config offset 0x1014 = width). Our impl returns `framebuffer.width` (=132 for Manuf.003). ✓ verified — trio with idx 69/71 |
| 71 | `sub_424FBF` | exen.Gamelet.getScreenHeight() → int (hash=0xd724a161, static native) | natives/exen/Gamelet.zig::getScreenHeight | canonical: `*a1 = sub_40223B()` → reads `*(dword_45FF3C+32)[1]` = device-descriptor[1], initialized from `dword_45C7C8` (device-config offset 0x1018 = height). Our impl returns `framebuffer.height` (=176 for Manuf.003). ✓ verified |
| 72 | `sub_424FF2` | exen.Gamelet.screenUpdate(int x, int y) → void (hash=0xbc1d842c, native instance, argc=2) | natives/exen/Gamelet.zig::screenUpdate | canonical: looks up `this.field[+28]` (offscreen Image descriptor) via `sub_426785`, calls `sub_402304(desc)` (device frame-flush — branches on depth, copies offscreen→device framebuffer). The 2 explicit args (likely dirty-rect hints) are NOT used in the canonical body. Our impl reads `FIELD_MAIN_OFFSCREEN (0x3dd39153)` from this, copies its `.pixels` row-by-row into host LCD framebuffer (`vm.framebuffer`). Both args ignored, matching canonical. Strings region row 40 confirms argc=2 (int, int) → void. Highest-frequency native (~109 calls/10s in Terminator = ~10.9 FPS). ✓ verified |
| 73 | `sub_424FD2` | exen.Gamelet.exitVm() → void (hash=0x3f52686f, native instance, argc=0) | natives/exen/Gamelet.zig::exitVm | canonical (ref:24989): `sub_406628(); sub_40777B(); *(*(dword_45FF3C+36)) = 1; return 0;` — stop audio + reset device + set "VM exiting" flag. Strings region row 41 `exitVm: () → void` matches (only argc=0 → void candidate near the exit cluster; row 44 onExit is also argc=0/void but canonical's audio-stop + flag-set is framework-side cleanup, not a user lifecycle callback). Our Zig: `core.audio.stop(); vm.exit_requested = true;` — frontend polls `vmExitRequested()` between ticks and quits gracefully. ✓ verified + implemented |
| 74 | `sub_42506C` | exen.Gamelet.throwInternalException(int code) → void (hash=0x305afb51, native static public, argc=1) | natives/exen/Gamelet.zig::throwInternalException | canonical (ref near 25072): `sub_434771("InternalExcept"); sub_406628(); sub_407A13(); return 0;` — print error string, stop audio, halt VM (non-catcheable abort). Strings region row 43 `throwInternalException: (int) → void` argc=1; method_table argc=1, BUT canonical's C-level body takes no args (ignores the bytecode-pushed int via the C calling-convention drop). Our Zig accepts `code: i32` but only logs + returns 0 (does NOT halt) — **documented divergence**: silencing the exception lets the gamelet keep running so we can observe later state instead of stopping at the first error. ✓ verified + implemented (with intentional non-canonical non-halt). |
| 75 | `sub_425090` | exen.Gamelet.getTimerTickCount() → int (hash=0xd7241463, native instance, argc=0) | natives/exen/Gamelet.zig::getTimerTickCount | canonical (ref:25046): `*a1 = sub_406872(); return 1;` — `sub_406872` returns `sub_435D3F()` which is `GetTickCount() - sub_43F5A0()` (Win32 ms-since-boot minus VM startup origin). Returns u32 milliseconds since gamelet started. Pikubi hammers this ~230×/session for animation timing. Our impl uses `std.time.milliTimestamp()` minus a lazy-initialised origin — bit-equivalent for the consumer (monotonic u32 ms-since-start). ✓ verified canonical-faithful |
| 76 | `sub_4250A3` | exen.Gamelet.startTimer(int period_ms) → void (hash=0x305a3e02, native instance, argc=1) | natives/exen/Gamelet.zig::startTimer | canonical: `sub_406851(arg[0])` → `sub_435D00(period_ms)` (Win32 SetTimer wrapper). Schedules the gamelet's per-tick callback at the given period. Our impl stores `period_ms` into `core.g_timer_period_ms`; the SDL frontend uses this to drive its tick rate. Strings region row 53 confirms argc=1 (int) → void. ✓ verified |
| 77 | `sub_4250BA` | exen.Gamelet.stopTimer() → void (hash=0x3f5219b9, native public, argc=0) | natives/exen/Gamelet.zig::stopTimer | canonical (ref:25115): `sub_406868(); return 0;` where sub_406868 → sub_435DDE = Win32 KillTimer wrapper. Strings region row 54 `stopTimer: () → void` matches (argc=0 void). Our Zig: `core.g_timer_period_ms = 0;` — clears the SDL frame-pacer period, the equivalent of clearing the Win32 timer. ✓ verified + implemented |
| 78 | `sub_4250C7` |  |  |  |
| 79 | `sub_425156` | exen.Gamelet.saveCtx(byte[] buf) → void (hash=0x6f6c1196, native instance, argc=1) | natives/exen/Gamelet.zig::saveCtx | canonical: validates byte[] non-null (else throws); calls `sub_4153F8(buf+20, length)` which caps at 300B, memcpy's FROM buf INTO device save area. Helper has embedded debug string `"exOnSaveCtx"` — direct name evidence. Symmetric pair to loadCtx (idx 80, `"exOnLoadCtx"`). Our impl: `core.eepromSave(bytes)` persists to `flash/eeprom.dat`. Strings region row 56 confirms argc=1 (byte[]) → void. ✓ verified |
| 80 | `sub_4251D4` | exen.Gamelet.loadCtx(byte[] buf) → void (hash=0x6f6c1c2e, native instance, argc=1) | natives/exen/Gamelet.zig::loadCtx | canonical: validates byte[] non-null (else throws); calls `sub_41547B(buf+20, length)` which caps at 300B, checks save-present sentinel, and memcpy's from device save area at `*(*(dword_45FF3C+20)+4)+60` into buffer. Our impl calls `core.eepromLoad(bytes)` to read from `flash/eeprom.dat` directly, mirrors into `inst.fields[1..]` for BALOAD parity. Strings region row 57 confirms: argc=1 byte[] → void. Trace string `aExonloadctx`="exOnLoadCtx" in canonical body confirms identity. ✓ verified |
| 81 | `sub_4253BC` |  |  |  |
| 82 | `sub_425252` | exen.Gamelet.playMelody(byte[] data) → void (hash=0x6f6cfd65, native public, argc=1) | natives/exen/Gamelet.zig::playMelody | canonical (ref:25147): `v2 = arg[0]; if (!v2) abort("playmelody error"); if (!global_audio_enabled) return 0; if (sub_406601(v2+20, *(WORD*)(v2+18)) != 1) halt; return 0;` — read buffer (data at +20, length-prefix at +18), send to device audio queue. Strings region candidate for argc=1 ref→void; the literal "playmelody error" string in the abort path confirms the name. Our Zig: delegates to `core.audio.play(bytes)`. ✓ verified + implemented |
| 83 | `sub_4252DF` | exen.Gamelet.stopMelody() → void (hash=0x3f527866, native public, argc=0) | natives/exen/Gamelet.zig::stopMelody | canonical (ref:25171): `sub_406628(); return 0;` — lone call to sound-cleanup helper. argc=0 void. Companion to playMelody (idx 82). Our Zig: delegates to `core.audio.stop()`. ✓ verified + implemented |
| 84 | `sub_4252EC` | exen.Gamelet.getNickName() → char[] (hash=0xb14f142d, native instance, argc=0) | natives/exen/Gamelet.zig::getNickName | canonical: copies 16 bytes from simulator's nickname slot (`dword_45FE8C+102`), strlen-trims, allocates a new char[] via `sub_410106(len, 0x150, "exen.Gamelet")` and returns the handle. The simulator initialises the slot to `"Xcell"` at boot (reference/ref:5350 in `sub_402A20` profile-init). Our impl returns a fresh char[] containing `"Xcell"` — matches canonical exactly for the default profile. Strings region row 63 confirms: argc=0 → char[]. ✓ verified |
| 85 | `sub_425372` |  |  |  |
| 86 | `sub_4253E3` |  |  |  |
| 87 | `sub_425427` | exen.Gamelet.getVersionInfo(String key) → String (hash=0x8a09c398, native static public, argc=1) | natives/exen/Gamelet.zig::getVersionInfo | canonical (ref:25232): scans `off_4492CC[2] = { "FrameWork", "V2.00" }` (ref:1508), byte-by-byte against the input String's char[]. On match returns a freshly-allocated String from the parallel `off_4492D0` table. Property-key lookup. Our Zig: compares input bytes against `"FrameWork"` literal; returns a new String containing `"V2.00"` on match, else null. Other keys not implemented (canonical's parallel table has only 2 entries and `(&off_4492D0)[1]` resolves to an unrelated symbol). ✓ verified + implemented |
| 88 | `sub_42563E` |  |  |  |
| 89 | `sub_429A18` |  |  |  |
| 90 | `sub_4298C8` |  |  |  |
| 91 | `sub_42993B` |  |  |  |
| 92 | `sub_429A20` |  |  |  |
| 93 | `sub_429AC0` |  |  |  |
| 94 | `sub_429B0A` |  |  |  |
| 95 | `sub_429B8D` |  |  |  |
| 96 | `sub_429C43` |  |  |  |
| 97 | `sub_429CB0` |  |  |  |
| 98 | `sub_429D2A` |  |  |  |
| 99 | `sub_429D86` |  |  |  |
| 100 | `sub_429E0A` |  |  |  |
| 101 | `sub_424940` |  |  |  |
| 102 | `sub_42497C` |  |  |  |
| 103 | `sub_424B70` | exen.FX.doRotozoomImage(image, graphics, x, y, angle, scale) → void (hash=0x2d3e3675, static native, argc=6) | natives/exen/FX.zig::doRotozoomImage | canonical (ref:24813): `v2 = sub_426785(graphics+24)`; `v3 = sub_426785(*(DWORD*)(image+24))`; null-check both (else `sub_434771("FX.doRotaZoom: image pointer might be null")` + non-catchable abort); dispatch into device-graphics vtable slot `*(v2[10]+64)(v2, v3, x, y, angle, scale)` — per-depth rotozoom kernel. Our Zig mirrors that math directly: identity fast-path when angle=0 ∧ scale=256 (centred blit), otherwise inverse-transform sample within a bounding box of `±max(src_w,src_h)*scale/256` around (x,y), using `std.math.sin/cos` with 8-bit angle convention (256 = full turn) and 256 = 1.0 fixed-point scale. Source palette is lazily decoded via `doTransformToSystemPalette`. Wallbreaker invokes ~32×/run for the ball trail + brick-burst FX. ✓ verified + implemented |
| 104 | `sub_424BFD` |  |  |  |
| 105 | `sub_424C84` |  |  |  |
| 106 | `sub_424D62` |  |  |  |
| 107 | `sub_424E40` |  |  |  |
| 108 | `sub_424ED2` |  |  |  |
| 109 | `sub_426870` |  |  |  |
| 110 | `sub_426990` |  |  |  |
| 111 | `sub_4269AC` |  |  |  |
| 112 | `sub_4269C8` |  |  |  |
| 113 | `sub_4269EB` |  |  |  |
| 114 | `sub_426A01` |  |  |  |
| 115 | `sub_426A14` |  |  |  |
| 116 | `sub_426A30` |  |  |  |
| 117 | `sub_426A4C` |  |  |  |
| 118 | `sub_426A68` |  |  |  |
| 119 | `sub_426A84` | exen.Math.abs(int) → int (hash=0xd82c715b, native static public, argc=1) | natives/exen/Math.zig::abs | canonical (ref:25892): `if (*a1 < 0) v2 = -*a1; else v2 = *a1; *a1 = v2; return 1;` — literal abs(). Our Zig: `return if (x < 0) -%x else x;` — exact bit-for-bit match (wrapping negation handles INT_MIN consistently with canonical's two's-complement behavior). ✓ verified + implemented |
| 120 | `sub_426AB4` | exen.Math.setRandSeed(int seed) → void (hash=0x305a2eee, static native, argc=1) | natives/exen/Math.zig::setRandSeed | canonical: `sub_41CD3D(*a1); return 0;` — call PRNG seed setter. Our impl stores seed into `Rng.state_a` and resets `Rng.state_b`. ✓ verified (rock-solid: full sig records, positional pairing locked across all 13 Math natives) |
| 121 | `sub_426ACA` | exen.Math.random() → int (hash=0xd7246024, static native, argc=0) | natives/exen/Math.zig::random | canonical: `*a1 = sub_41CD5D(); return 1;` — pair-state PRNG generator. Our impl: rotate state_a by (state_b & 0x1F), mix into state_b, add 31009 — direct port of canonical sub_41CD5D. ✓ verified |
| 122 | `sub_426ADD` |  |  |  |
| 123 | `sub_426B20` |  |  |  |
| 124 | `sub_426B89` |  |  |  |
| 125 | `sub_426BE3` |  |  |  |
| 126 | `sub_426C3D` |  |  |  |
| 127 | `sub_426C97` |  |  |  |
| 128 | `sub_426D7B` |  |  |  |
| 129 | `sub_42A020` | exen.Vector3D.squareLength() → int (hash=0xd72430ea, native public, argc=0) | natives/exen/Vector3D.zig::squareLength | canonical (ref:28091): `v3 = { this.x, this.y, this.z }; *a1 = sub_41CEB4(v3); return 1;` where `sub_41CEB4 = (z>>8)² + (y>>8)² + (x>>8)²`. Strings region row 2 `squareLength: () → int` matches. Our Zig: reads x/y/z from instance field_map (hashes 0xd042f048/0xd042e1c1/0xd042d35a) and computes the same shifted-byte squared sum. ✓ verified + implemented (companion to idx 130 length, which wraps this in `sub_41CE03 << 8`). |
| 130 | `sub_42A074` | exen.Vector3D.length() → int (hash=0xd724ffd6, native public, argc=0) | natives/exen/Vector3D.zig::length | canonical (ref:28108): copies x/y/z from this.field[6..8] into local `v3[3]`, calls `sub_41CEFA(v3)` = `sub_41CE03(x²+y²+z²) << 8` (fixed-point sqrt of sum of squares); clamps `-5` overflow to `-1`. Adjacent: idx 129 sub_42A020 = `squareLength` (same code, calls sub_41CEB4 instead — no sqrt). Pikubi calls this ~10×/game. ✓ verified + implemented (port includes canonical isqrt + squareLengthRaw helpers; triple cross-check: class hash 0xe36f9667 → Vector3D in extracted_table.md; method_table row 4 argc=0 → strings region row 3 "length"; canonical body computes sqrt). |
| 131 | `sub_42A0C8` |  |  |  |
| 132 | `sub_42A132` |  |  |  |
| 133 | `sub_42A1AD` |  |  |  |
| 134 | `sub_42A228` |  |  |  |
| 135 | `sub_42A28A` |  |  |  |
| 136 | `sub_42A305` |  |  |  |
| 137 | `sub_4284C9` |  |  |  |
| 138 | `sub_428683` |  |  |  |
| 139 | `sub_4286C9` |  |  |  |
| 140 | `sub_428716` |  |  |  |
| 141 | `sub_42874B` |  |  |  |
| 142 | `sub_42877A` |  |  |  |
| 143 | `sub_4287BE` |  |  |  |
| 144 | `sub_4288D3` |  |  |  |
| 145 | `sub_428910` |  |  |  |
| 146 | `sub_428962` |  |  |  |
| 147 | `sub_429F40` |  |  |  |
| 148 | `sub_429FB2` |  |  |  |
| 149 | `sub_429FBA` |  |  |  |
| 150 | `sub_429FC2` |  |  |  |
| 151 | `sub_42A660` |  |  |  |
| 152 | `sub_42A6CC` |  |  |  |
| 153 | `sub_42A718` |  |  |  |
| 154 | `sub_42A6DF` |  |  |  |
| 155 | `sub_42A3BE` | java.lang.Class.forName(String name) → Class (hash=0x70063066, static native, argc=1) | natives/java/lang/Class.zig::forName | canonical: `*a1 = sub_411795(*a1); return 1;` — `sub_411795` is the canonical "String name → class handle" resolver using the platform's custom hash. Our impl CRC32-hashes the String bytes (substitute for the unrecoverable the platform hash) and calls `vm.ensureClassObject(hash)` to get a stable Class<?> handle. Sentinel `0xFFFFFFFE` for empty-name fallback. ✓ verified |
| 156 | `sub_42A360` | java.lang.Class.newInstance() → Object (hash=0xd5710176, native instance, argc=0) | natives/java/lang/Class.zig::newInstance | canonical: checks `this.field[+28] (type tag) == 153` (Class type marker); if yes, calls `sub_410067(153, *(this+24)+12)` to allocate target-class instance; else throws IllegalArgumentException. Our impl: reads target_hash from `this.fields[0]`, allocates via `vm.heap.alloc(target_hash)`. Includes a bootstrap-singleton shortcut so `Bootstrap.statics[0]` keeps pointing at the pre-allocated gamelet handle during boot. ✓ verified |
| 157 | `sub_42A3DA` |  |  |  |
| 158 | `sub_42AC79` | java.lang.String.length() → int (hash=0xd724ffd6, native public, argc=0) | natives/java/lang/String.zig::length | canonical (ref:28618): `v3 = *(short**)(this+24); v4 = v3 ? *v3 : 0; *a1 = v4; return 1;` — reads u16 length-prefix at offset 0 of the char[] buffer. Strings region row 7 `length: () → int` matches. Our Zig: `return inst.bytes.len;` — equivalent (we store .bytes as the chars themselves; length = .bytes.len for normal Strings). NOTE: hash 0xd724ffd6 collides with Vector3D.length (CRC of "length") but disambiguated by class hash 0x7772dde3. ✓ verified + implemented |
| 159 | `sub_42A7B0` | java.lang.String.getBytes() → byte[] (hash=0xb14f9686, native public, argc=0) | natives/java/lang/String.zig::getBytes | canonical (ref:28407): reads `char[]` at `this+24` (2-byte length prefix), allocates a new `byte[]` of the same length (alloc type tag `0x9EC04138`), copies `byte[i] = (BYTE)char[i]` — direct low-byte truncation with no encoding transform. Adjacent idx 160 (sub_42A99F) applies a `tolower`/MBCS per-char transform (likely `getBytes(encoding)` or `toLowerCase().getBytes()`). ✓ verified + implemented (Zig port allocates byte[] with `BYTE_ARRAY_CLASS=0x9EC04138` tag matching canonical's `sub_410106` arg). |
| 160 | `sub_42A99F` |  |  |  |
| 161 | `sub_42A85E` | java.lang.String.compareTo(String other) → int (hash=0x1b487e6f, native instance, argc=1) | natives/java/lang/String.zig::compareTo | canonical (ref:25538): null-check other (else NPE); null-check this.bytes (else return -1); walk both `char[]` payloads in lockstep up to `min(this.len, other.len)` — return -1 if `this[i] < other[i]`, +1 if `this[i] > other[i]`. Once prefixes match, do a length tiebreak — but with a CANONICAL QUIRK: `if (other.length >= this.length) return (other.length > this.length) ? 1 : 0; else return -1`. That's the REVERSE of Java's standard `compareTo` length sign — canonical returns +1 when `this` is SHORTER and -1 when `this` is LONGER. We replicate the quirk verbatim because gamelets (Pikubi menu sort, Terminator's String-ctor 0x6f6cef2f-based hash lookups) depend on its sort order. Bytewise comparison is standard. Pikubi invokes ~1×/run, Wallbreaker doesn't use it. ✓ verified |
| 162 | `sub_42AA54` | java.lang.String.\<init>(String) → void (hash=0xf33e22cb, native instance, argc=1) | natives/java/lang/String.zig::ctorFromString | canonical (ref:25788): null-check src String (else NPE); if `src.char[]` non-null, clone via `sub_411ADD`, bump refcount, store new handle into `this.field[+32]` and `(length, payload_ptr)` at `+24..+32`; else (interned/borrowed) share src's buffer pointers directly. Our impl: `vm.allocator.alloc + @memcpy` into `this_inst.bytes`, mirror length in `fields[0]`, free any pre-existing `this.bytes`. The interned-borrowed branch maps to "empty src → empty copy" since our model has no shared/interned char[]s. Divergences: silent-return instead of NPE/OOM (no exception infra), no refcount (no GC). ✓ verified |
| 163 | `sub_42AAFC` | java.lang.String.\<init>(byte[]) → void (hash=0x35b022cb, native instance, argc=1) | natives/java/lang/String.zig::ctorFromBytes | canonical: null-check src byte[] (else NPE via sub_410198); if `src.length > 0`, clone byte[] via `sub_411ADD`, bump refcount, store new handle into `this.field[+32]` and (length, payload_ptr) at `+24..+32`. Our impl: vm.allocator.alloc + @memcpy into `this_inst.bytes`. Observable behavior identical (this owns a copy of src bytes). Divergences: silent-return instead of NPE (no exception infra), no refcount (no GC). ✓ verified |
| 164 | `sub_42AB8C` | java.lang.String.toUpperCase(String source) → void (hash=0x6f6c22cb, native instance, argc=1) | natives/java/lang/String.zig::toUpperCase | canonical (ref:28537): allocates a new byte[] sized to `source`'s char count, walks each byte through `unknown_libname_4` (libc toupper — pairs with idx 160 sub_42A99F which uses `unknown_libname_5`/tolower), writes the result into this's char[] slot at +32. CTOR-shape helper (mutates `this` from `source`), NOT a Java instance method despite the name. Our Zig: delegates to `mutateCase(this, source, std.ascii.toUpper)` — exact behavior (alloc → toupper per byte → swap into this.bytes). ✓ verified + implemented |
| 165 | `sub_42ACB6` | java.lang.String.Integer.toString(int) → String (hash=0xd045a46b, native static public, argc=1) | natives/java/lang/String.zig::integerToString | canonical (ref:28489): `v4 = alloc_String(); v5 = abs(*a1); count_digits; alloc char[]; sub_411490(*a1, char[]+20); String.value = char[]; *a1 = v4;` — int → String via itoa. method_table row 17 hash 0xd045a46b argc=1 static public native. Our Zig: `std.fmt.bufPrint("{d}", value)` + alloc String + copy bytes. Semantically equivalent (both produce decimal-digit String from int). ✓ verified + implemented |
| 166 | `sub_42AEEF` | java.lang.StringBuffer.initStringBuffer(int capacity) → void (hash=0x305ae3ec, native instance, argc=1) | natives/java/lang/StringBuffer.zig::ctorInit | canonical (ref:28737): `if (arg[0] > 0) sub_42AF2F(this, (u16)arg[0]); else sub_410198(127202840); return 0;` — alloc/resize this.value buffer to `cap` bytes; throw error on cap≤0. Strings region row 5 `initStringBuffer: (int) → void` matches (only argc=1→void candidate; row 6.1 `<init>(int)` returns ref). Our Zig: pre-allocates `cap` bytes in `inst.bytes` AND sets `inst.fields[0] = 0` (separate length tracker). Canonical-faithful since refactor split length-from-capacity. ✓ verified + implemented |
| 167 | `sub_42B17D` |  |  |  |
| 168 | `sub_42AE20` |  |  |  |
| 169 | `sub_42B056` | java.lang.StringBuffer.append(String) → StringBuffer (hash=0x5afc22cb, native public, argc=1) | natives/java/lang/StringBuffer.zig::appendString | canonical (ref:28860): null-check args; `v4 = src.value; sub_42AF2F(this, *(WORD*)v4)` to ensure capacity; `memcpy(this.value + 2 + this.length, v4 + 2, src.length); this.length += src.length;`. Strings region row 9 `append` (no sig records but argc=1 ref native matches). Our Zig: reads `src.fields[0]` for length, `ensureCapacity`, memcpy onto `inst.bytes[length..]`, bumps `fields[0]`. **Documented divergence**: canonical returns void but we return `this` Handle for chaining since our bridge doesn't push `this` after the native call. ✓ verified + implemented |
| 170 | `sub_42B0F2` |  |  |  |
| 171 | `sub_42B19F` |  |  |  |
| 172 | `sub_42B1ED` |  |  |  |
| 173 | `sub_42B244` |  |  |  |
| 174 | `sub_42AE4E` | java.lang.StringBuffer.toString() → String (hash=0xb2ba3939, native public, argc=0) | natives/java/lang/StringBuffer.zig::toStringOp | canonical (ref:28702): `v4 = sub_410067(153, 0x7772DDE3); v3 = sub_411ADD(this.value); if (v3) { ++refcount; new_string.value = v3; } else { sub_411B5E(v4); v4=0; throw OOM; } *a1 = v4; return 1;` — allocate new String, duplicate this.value (only the populated LENGTH bytes), return String handle. Strings region row 10 `toString` (no sig but argc=0→ref native matches). Our Zig: allocs String, copies `inst.bytes[0..inst.fields[0]]` (length-bounded), returns new handle. Post-refactor uses fields[0] for length, NOT bytes.len. ✓ verified + implemented |
| 175 | `sub_42B2A0` |  |  |  |
| 176 | `sub_42B2AD` |  |  |  |
| 177 | `sub_42B338` |  |  |  |
| 178 | `sub_4240A0` |  |  |  |
| 179 | `sub_4240E1` |  |  |  |
| 180 | `sub_424178` |  |  |  |
| 181 | `sub_424194` |  |  |  |
| 182 | `sub_4242B2` |  |  |  |
| 183 | `sub_424375` |  |  |  |
| 184 | `sub_4243C0` |  |  |  |
